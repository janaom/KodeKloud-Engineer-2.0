apiVersion: v1
kind: Pod
metadata:
  name: pod-nginx
  labels:
    app: nginx_app
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    command: ["sleep", "infinity"]

#then create the pod: kubectl create -f pod.yaml
